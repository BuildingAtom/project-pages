<div class="external-links" aria-label="Links">
  <div class="box">
  {%- for link in page.links %}
    <div class="single">
      <div class="icon">
      {%- include util/icons icon=link.icon icon-library=link.icon-library -%}
      </div>
      {%- if link.text or link.text != blank -%}
        <p>{{ link.text }}</p>
      {%- endif -%}
      {%- if link.url or link.url != blank -%}
        <a href="{{ link.url }}"></a>
      {%- endif -%}
    </div>
  {%- endfor %}
  </div>
</div>
